# Phase 2: 任务列表增强 - 任务清单

## 任务概览

| 任务 ID | 任务名称 | 改动文件数 | 代码量 | 状态 |
|---------|----------|-----------|--------|------|
| P2-T1 | 创建 TaskCard 任务卡片组件 | 2 | ~150 行 | 📋 |
| P2-T2 | 创建 TaskProgress 进度展示组件 | 2 | ~80 行 | 📋 |
| P2-T3 | 创建 TaskFilters 筛选器组件 | 2 | ~100 行 | 📋 |
| P2-T4 | 创建 TaskEmptyState 空状态组件 | 2 | ~60 行 | 📋 |
| P2-T5 | 优化 useTaskList Hook 支持轮询 | 1 | ~80 行 | 📋 |
| P2-T6 | 重构任务列表页面集成组件 | 2 | ~120 行 | 📋 |

---

## P2-T1: 创建 TaskCard 任务卡片组件

### 任务描述
创建任务卡片组件，根据任务状态展示不同的布局和操作

### 文件清单
- `apps/web/src/components/task/TaskCard.tsx` (新增)
- `apps/web/src/components/task/index.ts` (新增导出)

### 组件接口
```typescript
type TaskStatus = 'PENDING' | 'RUNNING' | 'COMPLETED' | 'FAILED' | 'PAUSED';

type TaskCardProps = {
  id: string;
  name: string;
  status: TaskStatus;
  type: 'SINGLE' | 'AB_TEST';

  // 运行中任务
  progress?: number;        // 0-100
  total?: number;
  completed?: number;
  passed?: number;
  failed?: number;
  estimatedTime?: string;   // "2分钟"

  // 已完成任务
  passRate?: number;        // 0-100
  duration?: string;        // "3分28秒"
  finishedAt?: string;      // "10分钟前"

  // 回调
  onView?: () => void;
  onCancel?: () => void;
  onRerun?: () => void;
  onExport?: () => void;
}
```

### 状态布局差异

**RUNNING 状态**:
- 显示进度条
- 显示统计指标（总数/已完成/通过/失败）
- 显示预估剩余时间
- 操作：查看详情、终止

**COMPLETED/FAILED 状态**:
- 显示通过率
- 显示耗时、完成时间
- 操作：查看详情、重新运行、导出报告

**PENDING 状态**:
- 显示等待中提示
- 操作：查看详情、取消

### 验收标准
- [ ] 各状态布局正确
- [ ] 操作按钮正常触发
- [ ] 卡片 hover 效果
- [ ] AB 测试类型有标识

---

## P2-T2: 创建 TaskProgress 进度展示组件

### 任务描述
创建可复用的进度展示组件，支持进度条和统计指标

### 文件清单
- `apps/web/src/components/task/TaskProgress.tsx` (新增)
- `apps/web/src/components/task/index.ts` (更新导出)

### 组件接口
```typescript
type TaskProgressProps = {
  total: number;
  completed: number;
  passed: number;
  failed: number;
  status: 'active' | 'success' | 'exception' | 'normal';
  showEstimate?: boolean;
  estimatedTime?: string;
}
```

### 布局结构
```
进度: ███████░░░ 72% (72/100)

┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│ 总数    │  │ 已完成   │  │ 通过    │  │ 失败    │
│ 100     │  │ 72      │  │ 68      │  │ 4       │
└─────────┘  └─────────┘  └─────────┘  └─────────┘

预估剩余: 2分钟
```

### 验收标准
- [ ] 进度条动画流畅
- [ ] 数值实时更新
- [ ] 支持成功/失败状态颜色

---

## P2-T3: 创建 TaskFilters 筛选器组件

### 任务描述
创建任务筛选器组件，包含搜索、类型、状态、时间范围

### 文件清单
- `apps/web/src/components/task/TaskFilters.tsx` (新增)
- `apps/web/src/components/task/index.ts` (更新导出)

### 组件接口
```typescript
type TaskFiltersValue = {
  search?: string;
  type?: 'SINGLE' | 'AB_TEST' | 'all';
  status?: TaskStatus | 'all';
  timeRange?: 'today' | 'week' | 'month' | 'all';
}

type TaskFiltersProps = {
  value: TaskFiltersValue;
  onChange: (value: TaskFiltersValue) => void;
  onCreateTask?: () => void;
}
```

### 布局结构
```
┌─────────────────────────────────────────────┐  ┌─────────────────┐
│ 🔍 搜索任务名称...                           │  │ + 创建任务 ▼   │
└─────────────────────────────────────────────┘  └─────────────────┘

筛选: [全部类型 ▼] [全部状态 ▼] [时间范围 ▼]            [刷新]
```

### 创建任务下拉菜单
- 新建测试任务
- 新建 A/B 测试

### 验收标准
- [ ] 搜索框实时过滤
- [ ] 下拉菜单正常工作
- [ ] 创建任务按钮使用渐变样式
- [ ] 刷新按钮触发列表刷新

---

## P2-T4: 创建 TaskEmptyState 空状态组件

### 任务描述
创建任务列表空状态组件，提供引导性内容

### 文件清单
- `apps/web/src/components/task/TaskEmptyState.tsx` (新增)
- `apps/web/src/components/task/index.ts` (更新导出)

### 组件接口
```typescript
type TaskEmptyStateProps = {
  filtered?: boolean;  // 是否是筛选后无结果
  onCreateTask?: () => void;
  onClearFilter?: () => void;
}
```

### 两种状态

**无任务**:
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│              ╭─────────────────────────────╮            │
│              │           ⚡                │            │
│              │                             │            │
│              │     还没有创建测试任务       │            │
│              │                             │            │
│              │   创建你的第一个测试任务，   │            │
│              │   验证 AI 模型的输出质量     │            │
│              │                             │            │
│              │   ┌─────────────────────┐   │            │
│              │   │   + 创建测试任务     │   │            │
│              │   └─────────────────────┘   │            │
│              │                             │            │
│              │   📖 查看使用文档           │            │
│              │                             │            │
│              ╰─────────────────────────────╯            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**筛选无结果**:
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│              ╭─────────────────────────────╮            │
│              │           🔍                │            │
│              │                             │            │
│              │     没有找到匹配的任务       │            │
│              │                             │            │
│              │   尝试调整筛选条件           │            │
│              │                             │            │
│              │   [清除筛选条件]            │            │
│              │                             │            │
│              ╰─────────────────────────────╯            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 验收标准
- [ ] 区分无数据和筛选无结果
- [ ] 按钮点击触发回调
- [ ] 样式符合设计规范

---

## P2-T5: 优化 useTaskList Hook 支持轮询

### 任务描述
优化任务列表 Hook，支持运行中任务的轮询更新

### 文件清单
- `apps/web/src/hooks/useTaskList.ts` (修改)

### Hook 接口
```typescript
type UseTaskListOptions = {
  filters?: TaskFiltersValue;
  pageSize?: number;
  pollInterval?: number;  // 轮询间隔，默认 3000ms
  enablePolling?: boolean; // 是否启用轮询
}

type UseTaskListReturn = {
  tasks: TaskItem[];
  loading: boolean;
  error: Error | null;
  pagination: {
    current: number;
    pageSize: number;
    total: number;
  };
  hasRunningTasks: boolean;
  refresh: () => void;
  setPage: (page: number) => void;
}
```

### 轮询逻辑
- 当存在 RUNNING 状态任务时自动启用轮询
- 轮询间隔可配置，默认 3 秒
- 页面不可见时暂停轮询
- 组件卸载时清理轮询

### 验收标准
- [ ] 轮询正常启动/停止
- [ ] 页面隐藏时暂停
- [ ] 进度数据实时更新
- [ ] 无内存泄漏

---

## P2-T6: 重构任务列表页面集成组件

### 任务描述
重构任务列表页面，集成所有新组件

### 文件清单
- `apps/web/src/app/(dashboard)/tasks/page.tsx` (修改)
- `apps/web/src/styles/tasks.module.css` (新增，可选)

### 页面结构
```tsx
export default function TasksPage() {
  const [filters, setFilters] = useState<TaskFiltersValue>({});
  const { tasks, loading, pagination, hasRunningTasks, refresh, setPage } = useTaskList({
    filters,
    enablePolling: true,
  });

  return (
    <div className="tasks-page">
      <PageHeader title="测试任务" />

      <TaskFilters
        value={filters}
        onChange={setFilters}
        onCreateTask={() => router.push('/tasks/new')}
      />

      {tasks.length === 0 ? (
        <TaskEmptyState
          filtered={hasFilters}
          onCreateTask={() => router.push('/tasks/new')}
          onClearFilter={() => setFilters({})}
        />
      ) : (
        <div className="task-list">
          {tasks.map(task => (
            <TaskCard
              key={task.id}
              {...task}
              onView={() => router.push(`/tasks/${task.id}`)}
              onCancel={() => handleCancel(task.id)}
              onRerun={() => handleRerun(task.id)}
            />
          ))}
        </div>
      )}

      <Pagination
        current={pagination.current}
        pageSize={pagination.pageSize}
        total={pagination.total}
        onChange={setPage}
      />
    </div>
  );
}
```

### 验收标准
- [ ] 页面布局符合设计
- [ ] 筛选功能正常
- [ ] 分页功能正常
- [ ] 轮询更新正常

---

## 开发日志

| 日期 | 任务 | 完成情况 | 备注 |
|------|------|---------|------|
| | | | |
