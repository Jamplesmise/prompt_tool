# 测试环境配置
# 使用独立的测试数据库，避免影响开发数据

# Database (测试数据库)
DATABASE_URL="postgresql://postgres:REDACTED_PG_PASSWORD@REDACTED_DB_HOST:REDACTED_PG_PORT/ai_eval_platform_test?directConnection=true"

# Redis
REDIS_URL="redis://default:REDACTED_REDIS_PASSWORD@REDACTED_DB_HOST:REDACTED_REDIS_PORT"

# MongoDB (测试数据库)
FASTGPT_MONGODB_URI="mongodb://root:REDACTED_MONGO_PASSWORD@REDACTED_DB_HOST:REDACTED_MONGO_PORT/fastgpt_test?authSource=admin&directConnection=true"
ENABLE_FASTGPT_MODELS="true"

# NextAuth
NEXTAUTH_SECRET="test-secret-key-32characters!!!"
NEXTAUTH_URL="http://localhost:3000"

# Sandbox
SANDBOX_URL="https://REDACTED_SANDBOX_HOST"
SANDBOX_API_KEY="dify-sandbox"

# Test Environment
NODE_ENV="test"
LOG_LEVEL="error"
ENCRYPTION_KEY="test-encryption-key-32characters!"
